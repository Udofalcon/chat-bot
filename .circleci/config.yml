version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:16.13.0
    steps:
      - checkout
      - run: npm install
  snyk:
    docker:
      - image: cimg/node:16.13.0
    steps:
      - run: npm run snyk
  test:
    docker:
      - image: cimg/node:16.13.0
    steps:
      - run: npm run test

workflows:
  main:
    jobs:
      - build
      - snyk:
          requires:
            - build
      - test:
          requires:
            - build
